---
title: "Page Not Found"
permalink: /404.html
---
{% extends "layouts/page.njk" %}

{% block pageContent %}

TODO duplicate on topics page - extract

{# Build an array of { name, count } for all tags #}
{% set tagStats = [] %}
{% for t in collections.tagList %}
{% set count = (collections[t] or []) | length %}
{% set _ = tagStats.push({ name: t, count: count }) %}
{% endfor %}

{# Sort by count desc, name asc on ties #}
{% set sorted = tagStats | sortByCountThenName %}



{% warning %}
  The page you are looking for does not exist.
  You can either try using search or browse all posts by category below.
{% endwarning %}

<h4>Search for posts</h4>
TODO SEARCH

<h4>All available posts by category</h4>
<ul class="tag-list">
  {% for t in sorted %}
  <li>
    <a href="/tags/{{ t.name | slugify }}/">{{ t.name }} ({{ t.count }})</a>
  </li>
  {% endfor %}
</ul>

{% endblock %}